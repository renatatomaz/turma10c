package decisao;

import javax.swing.JOptionPane;

public class DecisaoComposta {

	public static void main(String[] args) {
		/*
		 * Peçam o total de faltas
		 * Se o total de faltas for maior que 20, o aluno será reprovado.
		 */
		
		String nome = JOptionPane.showInputDialog("Digite seu nome: ").toUpperCase();
		byte faltas = Byte.parseByte(JOptionPane.showInputDialog("Informe o número de faltas"));
		float nota1 = Float.parseFloat(JOptionPane.showInputDialog("Nota 1: "));
		float nota2 = Float.parseFloat(JOptionPane.showInputDialog("Nota 2: "));
		float media = (nota1 + nota2)/2;
		
		if (faltas > 20) {
			System.out.println("Seu número de altas é acima do permitido, por isso você esta reprovado.\nSeu número de faltas é de "+ faltas);
		} else		
		if (media >= 6) {
			System.out.println("Aprovado com media: " + media);
		} else if (media < 4) {
			System.out.println("Infelizmente você foi reprovado: " + media);
		} else  {
			System.out.println("Você ainda tem uma chance no exame, sua média atual é " + media);
		}
				
		//and => &&
		// or => ||
				
		//else {
		//	System.out.println("Infelizmente você foi reprovado: " + media);
		//}
		
		}
}
